---
type: object
properties:
  address:
    type: string
  contact_id:         # may be empty if participant could not be linked to a user's contact
    type: string
  label:              # 'display-name' for email ; 'real name' for IRC
    type: string
  protocol:           # email, IRC…
    type: string
  type:               # for now, enum is RFC5322's field types for emails.
    type: string
    enum:
    - To
    - Cc
    - Bcc
    - From
    - Reply-To
    - Sender
required:
- address
- type
- protocol
additionalProperties: false
