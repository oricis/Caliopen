---
type: object
properties:
  actions:
    type: array
    items:
      type: string
      enum:
        - send
        - set_read
        - set_unread
        - reset_password
        - delete
        - device-validation
  params:
    type: object
additionalProperties: false
required:
  - actions
