---
type: object
properties:
  attachments:
    type: array
    items:
      "$ref": Attachment.json
  body: # in future, could be only a preview of body, limited to xxx chars
    type: string
  date:
    type: string
    format: date-time
  date_delete:
    type: string
    format: date-time
  date_insert:
    type: string
    format: date-time
  discussion_id:
    type: string
  external_references:
    type: object
    "$ref": ExternalReferences.json
  identities: # to which user's local identities the message is linked to
    type: array
    items:
      "$ref": LocalIdentity.json
  importance_level:
    type: integer
    format: int32
  parent_id: # The id of the Message this message is a reply to. This is primarily for drafts, but the server may support this for received messages as well by looking up the [@!RFC5322] Message-Id referenced in the In-Reply-To header and searching for this message in the user’s mail.
    type: string
  is_answered:
    type: boolean
  is_draft:
    type: boolean
  is_unread:
    type: boolean
  message_id:
    type: string
  privacy_features:
    type: object
    "$ref": PrivacyFeatures.json
  raw_msg_id:
    type: string
  recipients:
    type: array
    items:
      "$ref": Recipient.json
  subject:
    type: string
  tags:
    type: array
    items:
      "$ref": Tag.json
  type:
    type: string
  user_id:
    type: string
