---
type: object
properties:
  "$ref": NewTag.yaml#/properties
  tag_id:
    type: string
  type:
    type: string
    enum:
    - user
    - system
  name:             # the authoritative name given by the system, for system tags. Not editable by user (may not even be visible by user).
    type: string
required:
- name
- type
- label
additionalProperties: false

