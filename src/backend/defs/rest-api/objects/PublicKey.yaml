---
type: object
allOf:
- "$ref": NewPublicKey.yaml
- type: object
  properties:
    privacy_features:
      type: object
      "$ref": PrivacyFeatures.yaml
    pi:
      type: object
      "$ref": PI.yaml
    date_insert:
      type: string
      format: date-time
    date_update:
      type: string
      format: date-time
  required:
  - date_insert
  additionalProperties: false

